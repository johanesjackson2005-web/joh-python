app = 'joh-python'
primary_region = 'jnb'
console_command = '/code/manage.py shell'

[deploy]
release_command = 'python manage.py migrate --noinput'

[env]
PORT = '8000'
[processes]
app = "uvicorn myapp.asgi:application --host 0.0.0.0 --port 8000"

[http_service]
internal_port = 8000
force_https = true
auto_stop_machines = 'off'
auto_start_machines = true
min_machines_running = 1
processes = ['app']

[[vm]]
memory = '1gb'
cpu_kind = 'shared'
cpus = 1
memory_mb = 1024

[[statics]]
guest_path = "/app/staticfiles"
url_prefix = "/static/"